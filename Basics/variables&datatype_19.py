#Explore: create an integer variable and reassign it to a string, then to a list.
#Print the type after each assignment. What does this tell you about Python's type system?

Var1 = 2
print(type(Var1))
print(type(str(Var1)))
print(type([Var1]))

